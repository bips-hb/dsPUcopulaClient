% Generated by roxygen2: do not edit by hand
\name{ds.estimateMarginals}
\alias{ds.estimateMarginals}
\title{Estimate marginal distributions inside DataSHIELD}
\usage{
ds.estimateMarginals(x, datasources = NULL, method = "ecdf")
}
\arguments{
\item{x}{\code{character(1)}. The name of the server-side object that holds the
variables for which the marginals should be estimated.}

\item{datasources}{Optional list of DataSHIELD connections as returned by
\code{DSI::datashield.connections_find()}.}

\item{method}{\code{character(1)}. The marginal estimation approach to use. The
default is \code{"ecdf"}, but the value must match a method supported on the
server side.}
}
\value{
Invisibly returns \code{TRUE} once the remote object has been created.
}
\description{
Computes marginal distribution estimates for the variables identified by the
server-side symbol \code{x} through the \code{estimateMarginalsDS} helper. The
result is stored remotely as \code{marginals_list} for later reuse.
}
\examples{
\dontrun{
ds.estimateMarginals("PU_copula_model$data")
}
}
